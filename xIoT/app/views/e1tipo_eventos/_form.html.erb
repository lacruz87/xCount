<%= form_with(model: e1tipo_evento, local: true) do |form| %>
  <% if e1tipo_evento.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(e1tipo_evento.errors.count, "error") %> prohibited this e1tipo_evento from being saved:</h2>

      <ul>
      <% e1tipo_evento.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nombre %>
    <%= form.text_field :nombre, id: :e1tipo_evento_nombre %>
  </div>

  <div class="field">
    <%= form.label :ruta_bd %>
    <%= form.text_field :ruta_bd, id: :e1tipo_evento_ruta_bd %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
